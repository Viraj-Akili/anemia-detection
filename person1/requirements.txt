# PRAHARI AI/CV backend — runtime + dev dependencies (Hour 1, verified 2026-08-17)
# All packages below except pytest are already installed on this machine.

# --- API -------------------------------------------------------------------
fastapi>=0.110,<1.0          # installed: 0.137.2
uvicorn[standard]>=0.29      # installed: 0.49.0
python-multipart>=0.0.9      # installed: 0.0.32  (multipart uploads)
pydantic>=2.6                # installed: 2.13.4
python-dotenv>=1.0           # installed: 1.0.1   (.env loading)

# --- Image / numerics --------------------------------------------------------
numpy>=1.26,<2.0             # installed: 1.26.4
opencv-python>=4.9,<5        # installed: 4.13.0.92
pillow>=10.0                 # installed: 12.2.0
matplotlib>=3.8              # installed: 3.10.9 (explainability plots)

# --- Data / classical baseline -------------------------------------------------
scikit-learn>=1.4            # installed: 1.9.0   (baseline classifier + metrics)
pandas>=2.0                  # installed: 3.0.3   (dataset metadata)
openpyxl>=3.1                # installed: 3.1.5   (read the CP-AnemiC xlsx sheet)

# --- Deep learning --------------------------------------------------------------
# INSTALLED (Hour 4): torch 2.12.1+cu130, torchvision 0.27.1+cu130 (CUDA 13.0, RTX 5050).
# NOTE: to keep the GPU build, install wheels with --no-deps (a plain
# `pip install --force-reinstall torchvision` from the index can silently
# replace torch with the CPU build from PyPI):
#   pip install --force-reinstall --no-deps torch-2.12.1+cu130...win_amd64.whl torchvision-0.27.1+cu130...win_amd64.whl
torch==2.12.1                 # CPU fallback build is 2.12.1+cpu; GPU build is 2.12.1+cu130
torchvision==0.27.1           # CPU fallback build is 0.27.1+cpu; GPU build is 0.27.1+cu130

# --- Dev ---------------------------------------------------------------------
pytest>=8.0                  # NOT installed yet — needed for tests/
tqdm>=4.66                   # installed: 4.68.3 (training progress)
