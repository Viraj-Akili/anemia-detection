# Requirements for PRAHARI PPG Pipeline - Step 1 & Step 2
# Minimal dependencies only. No ML modeling frameworks (TensorFlow, PyTorch, XGBoost, etc.) are installed in Step 2.

pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
matplotlib>=3.7.0
pytest>=7.4.0
